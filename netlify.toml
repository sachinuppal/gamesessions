[build]
  command = "npm run build:web"
  publish = "dist"

# Handle SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

# Custom domain configuration
[context.production]
  environment = { SITE_URL = "https://games.house.games" }